>[success] # 用函数来创建私有属性
~~~
  var  myObject =( function () {
      var _name = 'seven'
      return {
          getName(){
              return _name
          }
      }
  })()
  console.log(myObject.getName()) // seven
  console.log(myObject._name) // undefined
~~~